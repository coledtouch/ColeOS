**/.git
**/.gitignore
.assetsignore
**/Thumbs.db
**/desktop.ini
*.tmp
*.bak
# sources and CI — version-controlled, never published
_src/
.github/
# local tooling config — never published
.claude/
# generated at deploy by _src/security-headers.js — never published as a file
_headers
# wrangler dev state (holds the dev machine's location) — never published
.wrangler/
